import React from 'react';

const PaymentDlg = ({ className, status }) => {
    return (
        <div className={className}>
            <div></div>
        </div>
    )
};

export default PaymentDlg;